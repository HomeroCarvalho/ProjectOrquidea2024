public class classe1
{
	
	
	public classe1()
	{

	};
	
	public int metodoContagem(int x)
	{
		for (int i=0;i<2;i++)
		{
			x=x+i*2;
		};
		
		return x;
	};
};

// linha com falha.
i=1;

// instanciacao de objeto.
classe1 obj1= create();
// atribuição a uma variavel.
int y = obj1.metodoContagem(3);
